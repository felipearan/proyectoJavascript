

/* function listarUsuarios() {
    let nodoElemento = document.createElement("h3")
    let nodoTexto = document.createTextNode("Nombres usuario")
    nodoElemento.appendChild(nodoTexto)
    document.getElementById("lista").appendChild(nodoElemento)

    for (let i = 0; i < usuarios.length; i++) {
        let nodoElemento = document.createElement("p")
        let nodoTexto = document.createTextNode(usuarios[i])
        nodoElemento.appendChild(nodoTexto)
        document.getElementById("lista").appendChild(nodoElemento)
    }
} */